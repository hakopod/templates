# Baserow

Complete split-service deployment with private, user-owned S3 media storage.
The preset includes PostgreSQL, authenticated Redis, the backend, frontend,
reverse proxy, combined Celery/export worker and the periodic beat scheduler.
The backend completes locked migrations before dependent workers/frontend start.
Every service runs as an unprivileged UID. No host socket, root initializer or
shared ReadWriteMany volume is required.

## Before deployment

1. Create a private S3-compatible bucket in your account. A dedicated bucket is
   recommended. Enable versioning/lifecycle or an independent backup appropriate
   to the provider. Hakopod does not create or take ownership of this bucket.
2. Give the application a bucket-scoped S3 credential with object read/write/delete
   and list permissions. Save its ID and secret as `storage-access-key` and
   `storage-secret-key`; these cannot be generated by Hakopod.
3. Supply the bucket name, HTTPS API endpoint and provider region (`auto` for R2).
   Configure bucket CORS for your exact HTTPS application origin and GET/HEAD
   requests so browsers can load attachments. Avoid a public wildcard bucket.
4. Generate `database-password`, `redis-password`, `session-secret` and
   `jwt-secret`. Configure a stable HTTPS application origin.
5. Review all seven service budgets. PostgreSQL and Redis each receive a PVC;
   uploads go to your bucket. Increasing replicas does not cluster the databases.

The mounted Django settings extension adds only the current pod's own addresses to Django's allowed hosts for Kubernetes readiness probes; it does not enable a wildcard host. It also disables object ACL headers and enables
15-minute signed URLs. This works with S3 Object Ownership and R2 without making
objects public. Customer credentials are environment references, never file content
or versioned TOML values. The standard backend S3 credential chain performs access.

The generated hostname must route to `main` (the reverse proxy); expose neither
PostgreSQL/Redis nor the internal backend directly. The proxy forwards API and
WebSocket paths; media URLs point directly to the configured bucket. Custom
builder domains and SMTP require additional upstream configuration. First-user
registration is upstream setup; finish it before sharing the application URL.

## Recovery

Back up PostgreSQL, retain the stable signing keys and protect the media bucket.
Redis persists queued work but is not a replacement for database or media backups.
This is a new-install preset; an existing installation needs its upstream data and
media migration procedure. Deleting the application does not delete the customer's
S3 bucket. Bucket lifecycle/costs remain under the customer account.

## Verification

Reviewed 24 September 2026 against the pinned 1.35.2 image configuration and source.
Full named-development-cluster startup and actual S3 media round-trip acceptance
are tracked separately; registry inspection alone does not prove them.

## Sources

- https://dokploy.com/templates/baserow
- https://github.com/bram2w/baserow/blob/1.35.2/docker-compose.yml
- https://github.com/bram2w/baserow/blob/1.35.2/Caddyfile
- https://github.com/bram2w/baserow/blob/1.35.2/backend/docker/docker-entrypoint.sh
- https://github.com/bram2w/baserow/blob/1.35.2/backend/src/baserow/config/settings/base.py
